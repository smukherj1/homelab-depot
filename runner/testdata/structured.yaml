server:
  listen_address: "127.0.0.1:9090"

process:
  command: ["echo", "structured"]
  working_directory: "./runner-process"

logs:
  directory: "./runner-logs"
  encoding:
    structured:
      format: "slog_json"
